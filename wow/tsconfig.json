{
  "compilerOptions": {
    "target": "ES2017",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "commonjs",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./src/*"
      ]
    },
    "allowSyntheticDefaultImports": true
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.d.ts",
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "src/**/*"
  ],
  "exclude": [
    "node_modules"
  ],
  "baseUrl": "./src", // ベースURLをsrcフォルダに設定
    "paths": {
      // @/ で始まるパスを src/ フォルダとして解決する
      // ただし、このエラーのモジュールは src/ ではなくプロジェクトルートの node_modules の隣にあると想定されるため、
      // 適切なパスを設定します。
      
      // パスエイリアスをプロジェクトルートを基準に設定する場合
      "@/*": ["./src/*"], 
      // Prismaのgeneratedファイルはプロジェクトルート基準で参照
      "@/generated/prisma": ["./node_modules/.prisma/client/index.d.ts"] // ★重要な設定★
    },
}
